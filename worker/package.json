{
  "name": "customer-portal-channels-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Persistent-connection worker for Zalo, Facebook, and Email channels. Reads outbound queue and delivers agent replies to external channels. Telegram uses webhooks on Vercel and does NOT require this worker.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "dotenv": "^16.4.5",
    "grammy": "^1.30.0",
    "zca-js": "^2.0.0-beta.32"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "tsx": "^4.19.2",
    "typescript": "~5.8.2"
  }
}
